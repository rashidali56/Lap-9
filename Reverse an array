#include <iostream>
using namespace std;

int main() {
    int n = 7;
    int arr[n] = {5, 10, 20, 30, 40, 50, 60};

    cout << "Reversed: ";
    for (int i = n - 1; i >= 0; i--) {
        cout << arr[i] << " ";
    }

    return 0;
}
